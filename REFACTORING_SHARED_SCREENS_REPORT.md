# Отчёт о рефакторинге: Вынос общих экранов в core:screens

**Дата:** 2025-11-18  
**Статус:** 🟡 В процессе (базовая структура создана)

---

## Краткий обзор

Начата работа по выносу общих экранов иерархии (Клиент → Объект → Установка → Компонент) из app-crm в отдельный модуль `core:screens` с централизованной системой проверки прав доступа. Создана базовая архитектура и начата реализация.

---

## Выполненные задачи

### 1. ✅ Создан модуль core:screens

**Файлы:**
- `core/screens/build.gradle.kts` - конфигурация модуля
- `core/screens/src/main/AndroidManifest.xml` - манифест модуля
- `settings.gradle.kts` - добавлен модуль в проект

**Зависимости:**
- `:core:ui` - базовые UI компоненты
- `:core:auth` - система авторизации и прав доступа
- Compose BOM, Material3, Lifecycle, Coroutines

**Статус:** ✅ Модуль создан и добавлен в проект

### 2. ✅ Создан HierarchyPermissionChecker

**Файл:** `core/auth/src/main/java/com/example/wassertech/core/auth/HierarchyPermissionChecker.kt`

**Функциональность:**
- Проверка прав доступа с учётом `user_membership` и `created_by_user_id`
- Поддержка ролей ADMIN, ENGINEER, CLIENT
- Методы для проверки прав на:
  - Просмотр/редактирование/удаление объектов (Sites)
  - Просмотр/редактирование/удаление установок (Installations)
  - Просмотр/редактирование/удаление компонентов (Components)
  - Создание новых сущностей (только если пользователь является создателем родительской сущности)
  - Изменение иконок

**Правила для CLIENT:**
- Видимость сущностей определяется через `user_membership` (если есть записи) или через `clientId`
- Редактирование разрешено только для сущностей с `created_by_user_id == currentUserId`
- Создание новых сущностей разрешено только если пользователь является создателем родительской сущности

**Правила для ADMIN/ENGINEER:**
- Полный доступ ко всем сущностям

**Статус:** ✅ Реализован и готов к использованию

### 3. ✅ Созданы UI State классы

**Файл:** `core/screens/src/main/java/ru/wassertech/core/screens/hierarchy/ui/HierarchyUiState.kt`

**Классы:**
- `SiteItemUi` - UI State для объекта (Site)
- `InstallationItemUi` - UI State для установки (Installation)
- `ComponentItemUi` - UI State для компонента (Component)
- `ClientSitesUiState` - UI State для экрана списка объектов клиента
- `SiteInstallationsUiState` - UI State для экрана списка установок объекта
- `InstallationComponentsUiState` - UI State для экрана списка компонентов установки

**Особенности:**
- Каждый UI State содержит флаги прав доступа (`canEdit`, `canDelete`, `canChangeIcon`)
- Отделены данные от логики прав доступа
- Готовы к использованию в shared-экранах

**Статус:** ✅ Созданы и готовы к использованию

### 4. 🟡 Создана базовая структура для shared-экранов

**Файл:** `core/screens/src/main/java/ru/wassertech/core/screens/hierarchy/ClientSitesScreenShared.kt`

**Реализовано:**
- Базовая структура shared-экрана для списка объектов клиента
- Принимает `ClientSitesUiState` и коллбеки для действий
- Поддержка режима редактирования
- FAB для добавления объектов (с проверкой прав)

**Требуется доработка:**
- Полная реализация отображения списка объектов
- Поддержка drag-and-drop
- Поддержка архивации/восстановления
- Поддержка изменения иконок
- Интеграция с существующими компонентами из app-crm

**Статус:** 🟡 Базовая структура создана, требуется доработка

---

## Требуется выполнить

### 1. ⏳ Вынести экраны из app-crm в core:screens

#### 1.1. ClientDetailScreen (ClientSitesScreenShared)
**Текущее состояние:** Базовая структура создана  
**Требуется:**
- Полная реализация отображения списка объектов с drag-and-drop
- Поддержка вложенных установок внутри объектов
- Поддержка архивации/восстановления
- Поддержка изменения иконок
- Интеграция с `SiteRowWithDrag` и `InstallationRowWithDrag` компонентами

**Файлы для изучения:**
- `app-crm/src/main/java/com/example/wassertech/ui/clients/ClientDetailScreen.kt` (1077 строк)

#### 1.2. InstallationsScreen (SiteInstallationsScreenShared)
**Требуется:**
- Создать shared-экран для списка установок объекта
- Поддержка drag-and-drop
- Поддержка архивации/восстановления
- Поддержка изменения иконок

**Файлы для изучения:**
- `app-crm/src/main/java/com/example/wassertech/ui/hierarchy/InstallationsScreen.kt`

#### 1.3. ComponentsScreen (InstallationComponentsScreenShared)
**Требуется:**
- Создать shared-экран для списка компонентов установки
- Поддержка drag-and-drop
- Поддержка архивации/восстановления
- Поддержка изменения иконок

**Файлы для изучения:**
- `app-crm/src/main/java/com/example/wassertech/ui/hierarchy/ComponentsScreen.kt` (708 строк)

#### 1.4. ClientsScreen (опционально)
**Требуется:**
- Создать shared-экран для списка клиентов (если нужен в app-client)
- Поддержка группировки по группам
- Поддержка drag-and-drop

**Файлы для изучения:**
- `app-crm/src/main/java/com/example/wassertech/ui/clients/ClientsScreen.kt` (1458 строк)

### 2. ⏳ Создать UserMembershipEntity и DAO

**Требуется:**
- Создать `UserMembershipEntity` в app-crm и app-client
- Создать DAO для работы с `user_membership`
- Добавить миграции БД для создания таблицы `user_membership`
- Реализовать синхронизацию `user_membership` с сервером

**Структура таблицы:**
```kotlin
@Entity(
    tableName = "user_membership",
    primaryKeys = ["user_id", "scope", "target_id"]
)
data class UserMembershipEntity(
    @ColumnInfo(name = "user_id") val userId: String,
    val scope: String, // "CLIENT", "SITE", "INSTALLATION"
    @ColumnInfo(name = "target_id") val targetId: String
)
```

### 3. ⏳ Реализовать логику создания/удаления user_membership

**Требуется:**
- При создании объекта пользователем CLIENT - создавать запись в `user_membership`
- При создании установки пользователем CLIENT - создавать запись в `user_membership`
- При удалении установки - удалять соответствующие записи в `user_membership`
- Интегрировать с SyncEngine для синхронизации

**Места для изменений:**
- `HierarchyViewModel.addSite()` - при создании объекта
- `HierarchyViewModel.addInstallation()` - при создании установки
- `HierarchyViewModel.deleteInstallation()` - при удалении установки
- SyncEngine - для синхронизации с сервером

### 4. ⏳ Обновить app-crm для использования shared-экранов

**Требуется:**
- Создать ViewModel-обёртки, которые формируют UI State из данных БД
- Обновить навигацию для использования shared-экранов
- Убедиться, что поведение CRM не изменилось (полные права инженера/админа)

**Подход:**
- ViewModel в app-crm загружает данные из БД
- ViewModel формирует UI State с флагами прав доступа (для CRM - все права = true)
- ViewModel передаёт UI State в shared-экран
- Shared-экран отображает UI на основе state

### 5. ⏳ Обновить app-client для использования shared-экранов

**Требуется:**
- Создать ViewModel-обёртки, которые:
  - Загружают данные из БД
  - Фильтруют по `user_membership`
  - Определяют права доступа через `HierarchyPermissionChecker`
  - Формируют UI State с правильными флагами прав
- Обновить навигацию для использования shared-экранов
- Убедиться, что ограничения прав работают корректно

**Экраны для обновления:**
- `SitesScreen` (стартовый экран после логина) → использовать `ClientSitesScreenShared`
- `SiteDetailScreen` → использовать `SiteInstallationsScreenShared`
- `ComponentsScreen` → использовать `InstallationComponentsScreenShared`

### 6. ⏳ Тестирование

**Требуется:**
- Проверить сборку проекта
- Проверить работу экранов в app-crm (должно работать как раньше)
- Проверить работу экранов в app-client (с ограничениями прав)
- Проверить создание/удаление `user_membership`
- Проверить синхронизацию `user_membership` с сервером

---

## Архитектура решения

### Структура модулей

```
core:screens
├── hierarchy/
│   ├── ui/
│   │   └── HierarchyUiState.kt (UI State классы)
│   ├── ClientSitesScreenShared.kt (shared-экран списка объектов)
│   ├── SiteInstallationsScreenShared.kt (shared-экран списка установок)
│   └── InstallationComponentsScreenShared.kt (shared-экран списка компонентов)
└── clients/
    └── ClientsScreenShared.kt (опционально, если нужен в app-client)

core:auth
└── HierarchyPermissionChecker.kt (проверка прав доступа)
```

### Поток данных

```
app-crm / app-client
    ↓
ViewModel (загружает данные из БД, применяет фильтры и проверки прав)
    ↓
Формирует UI State (с флагами прав доступа)
    ↓
core:screens (shared-экран)
    ↓
Отображает UI на основе state
```

### Проверка прав доступа

```
UserSession (текущий пользователь)
    ↓
HierarchyPermissionChecker (проверяет права)
    ↓
UserMembership (список memberships из БД)
    ↓
Entity (site/installation/component с created_by_user_id и origin)
    ↓
Результат: canView, canEdit, canDelete, canChangeIcon
```

---

## Изменённые файлы

### Новые файлы:
1. `core/screens/build.gradle.kts` - конфигурация модуля
2. `core/screens/src/main/AndroidManifest.xml` - манифест модуля
3. `core/auth/src/main/java/com/example/wassertech/core/auth/HierarchyPermissionChecker.kt` - проверка прав доступа
4. `core/screens/src/main/java/ru/wassertech/core/screens/hierarchy/ui/HierarchyUiState.kt` - UI State классы
5. `core/screens/src/main/java/ru/wassertech/core/screens/hierarchy/ClientSitesScreenShared.kt` - базовая структура shared-экрана

### Изменённые файлы:
1. `settings.gradle.kts` - добавлен модуль `:core:screens`

---

## Рекомендации по дальнейшей работе

### Приоритет 1: Завершить вынос экранов
1. Завершить реализацию `ClientSitesScreenShared` (полная реализация с drag-and-drop)
2. Создать `SiteInstallationsScreenShared`
3. Создать `InstallationComponentsScreenShared`

### Приоритет 2: Реализовать user_membership
1. Создать `UserMembershipEntity` в app-crm и app-client
2. Создать DAO для работы с `user_membership`
3. Добавить миграции БД
4. Реализовать логику создания/удаления записей

### Приоритет 3: Интеграция
1. Обновить app-crm для использования shared-экранов
2. Обновить app-client для использования shared-экранов
3. Протестировать работу обоих приложений

### Подход к работе:
1. Работать поэтапно, по одному экрану
2. После каждого этапа проверять сборку проекта
3. Тестировать изменения в обоих приложениях
4. Сохранять обратную совместимость

---

## Заключение

Создана базовая архитектура для выноса общих экранов в модуль `core:screens` с централизованной системой проверки прав доступа. Реализованы ключевые компоненты:

- ✅ Модуль `core:screens` создан и настроен
- ✅ `HierarchyPermissionChecker` реализован с поддержкой `user_membership` и `created_by_user_id`
- ✅ UI State классы созданы и готовы к использованию
- 🟡 Базовая структура shared-экрана создана, требуется доработка

**Следующие шаги:**
1. Завершить реализацию shared-экранов
2. Реализовать `user_membership` в БД
3. Интегрировать shared-экраны в app-crm и app-client
4. Протестировать работу обоих приложений

---

**Автор:** AI Assistant (Cursor)  
**Дата:** 2025-11-18

