package com.example.wassertech.data.types

enum class ComponentType { FILTER, RO, COMPRESSOR, AERATION, DOSING, SOFTENER }
enum class FieldType { CHECKBOX, NUMBER, TEXT }
enum class Severity { LOW, MEDIUM, HIGH }
